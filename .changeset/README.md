# Changesets

This folder holds [Changesets](https://github.com/changesets/changesets). For a
user-facing change, run `npx changeset`, describe the change, and commit the
generated file. Release automation (`npx changeset version` → `npm publish`)
consumes them to bump the version and write the changelog.
